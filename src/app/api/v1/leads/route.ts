/**
 * @file route.ts
 * @description Endpoint de Ingestão de Leads REST v1 (POST /api/v1/leads).
 *
 * Provê a interface canônica de ingestão de leads da API v1 do Acelera Auto CRM.
 */

export { POST } from "./ingest/route";
